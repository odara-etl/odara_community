# Showcase assets

Media referenced by the root `README.md`. These were captured from a real,
locally running Odara instance (v0.1.0).

| File | Used in | Notes |
|------|---------|-------|
| `demo.gif` | README hero | Open a pipeline → run it → open the AI assistant (~10s loop). |
| `screenshots/editor.png` | README | The React Flow editor with a 3-node pipeline loaded |
| `screenshots/run-progress.png` | README | A finished run ("Completed") with node stats |
| `screenshots/ai-assistant.png` | README | The AI assistant generating a node |

To regenerate, re-record against a running instance and keep the GIF under
~10 MB so it loads quickly on GitHub.
