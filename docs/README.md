# Showcase assets

Media referenced by the root `README.md`. Replace the placeholders below with
real files.

| File | Used in | Notes |
|------|---------|-------|
| `demo.gif` | README hero | Short loop (10–20s) of building + running a pipeline. Keep it under ~10 MB so it loads on GitHub. |
| `screenshots/editor.png` | (optional) | The React Flow editor with a pipeline open |
| `screenshots/ai-assistant.png` | (optional) | The AI assistant generating a node |
| `screenshots/run-progress.png` | (optional) | Live run progress / streaming |

Tips:
- Record at a 16:9 or similar ratio; trim dead air.
- For the GIF, ~1280px wide is plenty for README rendering.
